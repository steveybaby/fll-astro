# Cloudflare R2 Configuration for Photo Storage
# Copy this file to .env and fill in your actual values

# Your Cloudflare Account ID (found in Cloudflare dashboard)
R2_ACCOUNT_ID=your_account_id_here

# For Local Development (User API Keys)
R2_ACCESS_KEY_ID=your_user_api_access_key_id
R2_SECRET_ACCESS_KEY=your_user_api_secret_access_key

# R2 Bucket Configuration
R2_BUCKET_NAME=fll-photos
R2_PUBLIC_URL=https://fll-photos.your-domain.com

# Immich (for `npm run sync-immich YYYY-MM-DD`)
# Base URL of your Immich server and a user API key (Account Settings → API Keys)
IMMICH_URL=https://photos.your-domain.com
IMMICH_API_KEY=your_immich_api_key

# Notes:
# - For GitHub Actions, these should be stored as GitHub Secrets
# - Use Account API keys for GitHub Actions (more reliable)
# - Use User API keys for local development (more secure)
# - The public URL should point to your R2 bucket's custom domain